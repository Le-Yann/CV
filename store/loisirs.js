import {loisirs} from "~/assets/loisirs.js"
export const state = () => loisirs;

export const mutations = {
  // add(state, text) {
  //   state.list.push({
  //     text,
  //     done: false
  //   });
  // },
  // remove(state, { todo }) {
  //   state.list.splice(state.list.indexOf(todo), 1);
  // },
  // toggle(state, todo) {
  //   todo.done = !todo.done;
  // }
};
